<%= form_for(@tool, url: target) do |f| %>
  <%= f.label "name:" %> 
  <%= f.text_field :name %>
  <br>

  <%= f.label "use:" %> 
  <%= f.text_field :use %>
  <br>

  <%= f.label "user_id:" %> 
  <%= f.text_field :user_id %>
  <br>

  <%= f.submit %> 
<%end%>
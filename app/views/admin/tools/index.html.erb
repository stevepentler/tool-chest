<h1>Tools Index</h1>
<% @tools.each do |tool| %>
  <h3><%= tool.name %></h3>
  <h5><%= tool.use %></h5>

<%= link_to "New tool", new_admin_tool_path%>
  <!-- <p><h3><%#=link_to(tool.name, user_tool_path(@user, tool)) %></h3> <h5> (<%= tool.use %>) </h5> -->
  <%#= link_to "edit", edit_user_tool_path(@user, tool) %>
  <!-- <%#= link_to "delete", user_tool_path(@user, tool), method: :delete %></p> -->
<%end%>
<br>
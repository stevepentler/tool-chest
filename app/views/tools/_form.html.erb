<%= form_for(@tool, url: user_tools_path) do |f| %>
  <%= f.label "name:" %> 
  <%= f.text_field :name %>
  <br>

  <%= f.label "use:" %> 
  <%= f.text_field :use %>
  <br>

  <%= f.submit %> 
<%end%>